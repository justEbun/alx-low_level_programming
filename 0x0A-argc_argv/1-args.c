#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of argumentd passed into it
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
